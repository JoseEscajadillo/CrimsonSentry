# Rust's output directory
target

# Local settings
.soroban
.stellar

# demo scratch
evidence/last-error.log

# soroban test snapshots (regenerated by cargo test)
test_snapshots/

# Node tools (scanner / demo agent)
node_modules/
.env
